# 困境书单 — 项目交接文档

> 给接手这个项目的 agent / 开发者看的说明书。读完这一份，就能独立改内容、发版本。

---

## 1. 这是什么

**困境书单**：用户用一句话描述自己当下的困境（焦虑、拖延、职业迷茫、原生家庭创伤……），
产品匹配到对应主题，先问 1 个补充问题，再给出书单——**经典书**（提供深层理解框架）+ **近年热门书**（低门槛入门），
每本书标注阅读难度、豆瓣评分、Goodreads 评分、推荐理由，并附微信读书搜索链接。

定位：**TRAE AI 创造力大赛参赛作品**。纯前端、无后端、无账号、无数据库，静态托管。

---

## 2. 仓库与线上地址

| 项 | 值 |
|---|---|
| GitHub 仓库 | `https://github.com/nvnv19309/kunjing-shudan`（public，默认分支 `main`） |
| 线上地址 | `https://nvnv19309.github.io/kunjing-shudan/` |
| 托管方式 | GitHub Pages，source = `main` 分支 `/` 根目录 |
| 部署状态 | built（推送 `main` 后自动构建，约 1 分钟生效） |

⚠️ **注意**：仓库根目录直接放应用文件（`index.html` / `app.js` / `books-data.js` / `style.css`），
所以线上地址是仓库根路径。**不要在仓库里再套一层目录**，否则 Pages 会 404。

---

## 3. 文件地图

### 3.1 线上应用（真正要维护的东西）

工作区源码目录：`kunjing-shudan-app/`（**这是唯一的编辑入口**）

| 文件 | 行数 | 职责 |
|---|---|---|
| `index.html` | 128 | 页面骨架：三个步骤（输入 → 补充问题 → 结果）、未命中候选区、风险提示框、选择回显、难度图例、页脚免责声明 + 使用数据入口 |
| `books-data.js` | 832 | **核心数据库**：14 个主题（5 个预警）、62 本书、376 关键词、14 组补充问题（含选项语义标签）、`TOPIC_LIST` 生成 |
| `app.js` | 516 | 交互逻辑：主题卡片渲染、关键词匹配（大小写归一 + 去冗余 + 并列判定）、未命中候选、问答流程、按答案排序书区、**本地使用日志**、书卡渲染、步骤切换 |
| `style.css` | 591 | 视觉系统：暖色系（暖米色背景 + 奶油白卡片 + 鼠尾草绿主色 + 暖琥珀强调），衬线标题 |
| `.gitattributes` | 3 | 换行符规范：`* text=auto eol=lf`，仓库与检出统一 LF。**不要删**，删了会重新出现 CRLF 幽灵 diff |

### 3.2 参赛展示页（一般不用动）

| 路径 | 说明 |
|---|---|
| `kunjing-shudan/kunjing-shudan.html` | 565 行，单页长文报名页。含「创意名称与介绍 / 目标用户及痛点 / 产品核心逻辑 / 推荐标准 / 价值与意义 / 报名话题帖内容 / 内容来源」7 个板块 |
| `kunjing-shudan/_shared/fonts/` | 展示页内嵌字体（InstrumentSans、JetBrainsMono） |
| `kunjing-shudan.zip` | 展示页的打包产物 |
| `TRAE_AI创造力大赛_最终创意内容.docx` | 参赛创意内容的 Word 稿 |

---

## 4. 数据 Schema（改内容必读）

`books-data.js` 结构：

```js
const BOOKS_DATA = {
  <topicKey>: {                  // 英文 key，如 anxiety / mood / family
    title: "焦虑与压力",          // 主题中文名，显示在卡片上
    icon: "💔",                   // 卡片 emoji
    keywords: ["焦虑", "压力", ...],  // 用于匹配用户输入。注意：不是越多越好，见 §6.1 与 §10.2
    questions: [                  // 补充问题；可为空数组 []，为空则跳过第二步
      {
        id: "need",
        text: "你现在更需要什么？",
        options: ["理解焦虑本身", "缓解方法", "两者都要"],
        optionHints: ["classic", "popular", null],  // 与 options 等长、一一对应，见下方说明
        escalateFrom: 2                             // 可选。选题下标 >= 此值时升级风险提示
      }
    ],
    warning: true,                // 可选。严重议题必须加，见第 5 节
    classic: [ /* book */ ],      // 经典书
    popular: [ /* book */ ]       // 近年热门书
  }
};
```

**`optionHints` / `escalateFrom` 是什么**（2026-09-25 新增）

这两项是**补充问题的答案真正影响输出的唯一入口**。以前答案收上来就被丢掉了——
用户答完一题，结果页和没答时一模一样，提问纯属摩擦。

| 字段 | 取值 | 效果 |
|---|---|---|
| `optionHints[i]` | `"classic"` | 用户选第 i 个选项时，「经典书」区排在前（默认行为） |
| | `"popular"` | 「近年热门」区排到最前——适合"我想要方法/怎么缓解"类选项 |
| | `null` | 不改变顺序（等同 classic） |
| `escalateFrom` | 数字，如 `2` | 选到第 2 个及之后的选项时，风险提示框里追加一段「先求助、不必等看完书」 |

**为什么写死在数据里、不在 JS 里判断选项语义**：在 JS 里按选项文字猜"这是想要方法还是想要理解"
= 再叠一层脆弱的关键词匹配，而且没法审。写成数据字段，改哪一条一目了然。

`escalateFrom` 目前用在 3 处，都是**安全相关**的题：
`mood`（持续时间超过一个月起）、`personality`（"如何寻求帮助"）、`health`（"有过不好的就医经历"）。

**改这两项时必须保证 `optionHints.length === options.length`**，且顺序一一对应——
`verify-kunjing.js` 会校验，长度不符会报错。

```js
// 主题列表自动生成 —— 卡片顺序 = BOOKS_DATA 的键顺序
const TOPIC_LIST = Object.keys(BOOKS_DATA).map(key => ({
  key, title: BOOKS_DATA[key].title, icon: BOOKS_DATA[key].icon
}));
```

单本书结构：

```js
{
  title: "焦虑的意义",
  author: "罗洛·梅",
  year: 1950,
  difficulty: "进阶",        // 只能是 "入门" / "进阶" / "较难"
  douban: "8.6",            // 字符串，不是数字
  goodreads: "4.24",        // 字符串，不是数字
  desc: "存在主义心理学经典……",   // 这是什么书（客观描述）
  reason: "如果你想从根本上理解……" // 为什么推荐给你（第二人称，贴近用户处境）
}
```

**写 `reason` 的调性**：不要复述 `desc`，要说"如果你正处在 X 状态，这本书能给你 Y"。
现有数据全部是第二人称、口语化、避免说教的写法，保持一致。

---

## 5. 硬约束（不可违反）

1. **全免费**：托管只用 GitHub Pages，不引入付费服务、后端、数据库、账号系统。
2. **严重心理议题必须加 `warning: true`**：
   页面会显示风险提示框，引导用户联系心理援助热线（全国 24 小时：400-161-9995）或就医。
   当前 4 个主题已启用：`mood`（情绪低落与抑郁倾向）、`personality`（人格障碍与自我怀疑）、
   `health`（健康焦虑与就医恐惧）、`family`（原生家庭与童年创伤）。
   **新增任何涉及抑郁、自伤、人格障碍、创伤、成瘾、进食障碍的主题，都必须加这个字段。**
3. **免责声明不可删除**：页脚"书籍不能替代专业帮助"、结果页风险提示框，都必须保留。
4. **评分必须真实**：豆瓣分与 Goodreads 分要与实际平台一致，**不得编造**。数据以书籍条目为准。
5. **纯前端约束**：不引入构建工具、npm 依赖、框架。原生 HTML/CSS/JS，`<script>` 直引。

---

## 6. 推荐逻辑（`app.js`）

1. **主题卡片点击** → `selectTopic(key)`
2. **文字输入** → `matchTopic(text)`：先做大小写归一与去重，再逐主题 `indexOf` 命中、**去冗余后计分**，
   最后按并列规则定胜负（细则见 6.1）。
3. **未命中** → `showNoMatch()`：换 placeholder + 主题区域 pulse 动画，
   **并给出最接近的 3 个主题供点击**（细则见 6.3）。
4. **补充问题** → `renderQuestions()` 渲染选项，全部答完才解锁「查看推荐」按钮。
5. **结果页** → 先回显用户的选择，再按 `optionHints` 决定两个书区的先后，
   按 `escalateFrom` 决定要不要升级风险提示（细则见 6.4），然后 `renderBooks()` 渲染书卡；
   `difficulty` 映射到样式：`入门 → badge-easy`、`进阶 → badge-mid`、`其余 → badge-hard`。
6. **微信读书链接**：`https://weread.qq.com/#search/<书名>`，`encodeURIComponent` 编码后拼 URL。

### 6.1 计分与并列判定规则（2026-09-24 定 / 2026-09-25 补第 1 步）

`matchTopic()` 与 `scoreTopics()` 共用同一套计分，分四步。规则写在 `app.js` 里，注释与本节一一对应。

**第 0 步：大小写归一 + 去重（2026-09-25 新增）**

`text` 早就会 `toLowerCase()`，但关键词从来没有——于是数据里那几个大写 ASCII 关键词
（`BPD` / `NPD` / `ASPD` / `CPTSD` / `PUA` / `HSP`）**永远匹配不上，是死词**。
当时靠"大小写各写一遍"（`BPD` + `bpd`）掩盖了问题，但那本身就是隐患：
只写一种大小写就会静默失效。

现在把关键词也 `toLowerCase()`，同时按小写形态去重——否则 `BPD` 和 `bpd` 会各算一次命中（重复计分）。

**第 1 步：去冗余（同一主题内不重复计分）**

同一主题里，若某个命中词被本主题**另一个更长的命中词**包含，就丢掉不计数。
因为那是同一处证据被数了两遍。

> 反例：输入「晚上睡不着」时，`mood` 的「睡不着」和「睡」都命中。
> 不去冗余就白得 1 分，让关键词多的主题虚高。
> **实测这句话会把 `anxiety` 误判给 `mood`**——「焦虑 / 担心」2 分，
> 「睡不着 / 睡」也算 2 分，再靠下面的第 2 条规则翻盘，判成「情绪低落」。
> 去冗余后 `mood` 只剩「睡不着」1 分，判回 `anxiety`。
>
> 这类冗余集中在几个单字/短词上：`睡`（被 12 个更长词包含）、`钱`（7 个）、
> `父母`（4 个）、`家庭` / `医院` / `敏感` / `自己`（各 3 个）。
> **`睡` 已在 2026-09-25 从数据里删掉**——它被本主题 12 个更长词完全覆盖，
> 只在「睡衣 / 睡懒觉」这类输入上误触发，留着没有任何收益。

**第 2 步：多主题得分相同时，按三条依次判定**

| 优先级 | 规则 | 为什么 |
|---|---|---|
| 1 | **命中关键词数多者胜** | 命中越多越像在说这个困境 |
| 2 | 命中数相同时，**命中的最长关键词更长者胜** | 说法越具体越该优先：「得了大病」（4 字）压过「自己」（2 字） |
| 3 | 仍相同时，**按 `BOOKS_DATA` 的键顺序**，先出现的主题胜出 | 兜底，保证结果稳定可复现 |

第 2 条是 2026-09-24 新增的。**没有它时，第 3 条的键顺序会静默决定胜负**——
实测「总觉得自己得了大病，不敢去体检」会因为这个被判给 `self`（靠关键词「自己」），
而不是本该命中的 `health`。有了第 2 条，`health` 靠「得了大病」胜出。

**第 3 步：实现细节 —— 用排序而不是遍历打擂**

`scoreTopics()` 给 14 个主题各算一份 `{score, longest, order}`，然后**排序**取第一名。
这样「未命中时给候选」可以直接复用同一个函数（见 6.3），不必再写一套打分。
排序的兜底键是 `order`（即 `BOOKS_DATA` 的键顺序），行为与旧的遍历打擂完全一致。

**关于裸泛词：哪些留、哪些删（都有实测依据，别凭感觉动）**

| 词 | 所在主题 | 处理 | 依据 |
|---|---|---|---|
| `自己` | `self` | **保留** | 删掉会让留出集准确率从 25.7% 掉到 **20.0%**：未命中从 36 条涨到 46 条。`self` 的真阳性（如「常常怀疑自己到底行不行」）全靠它兜住。它引起的误判交给第 2 条规则压 |
| `关系` | `relationship` | **已删**（09-24） | 它是「一句话同时命中两个主题」的元凶，且极其泛（「跟父母关系很差」说的是家庭） |
| `效率` / `集中不了` | `procrastination` | **已删**（09-25） | 与 `learning` 逐字重复。这两个词更该归「学习方法与效率」，留着只会让结果由键顺序随机决定 |
| `被抛弃` | `personality` | **已删**（09-25） | 与 `breakup` 逐字重复。删的同时补了「怕被抛弃」：于是「怕被抛弃」→ 人格障碍，光秃秃的「被抛弃」→ 失恋，**避免心碎的人被送进「人格障碍」** |
| `崩溃` | `anxiety` | **移到 `mood`**（09-25） | 「很崩溃，眼泪止不住」应归情绪低落；「崩溃」本身更像情绪状态而非焦虑症状 |
| 给泛词降权（0.5 分） | — | **不做** | 实测零收益，纯增复杂度 |

### 6.2 匹配是朴素子串匹配，别指望它做语义

> 没有分词、没有语义。用户输入里**连续出现** `keywords` 中的词才算命中。
> 插入副词就会失效：「情绪特别不稳定」不命中关键词「情绪不稳定」。
> 发现某类困境匹配不到，**先补 `keywords`**；要动匹配算法（如去冗余与并列规则），
> **必须先在留出集上跑出正收益**再改——方法论与数据见 §10.2。

### 6.3 未命中时给候选（2026-09-25 新增）

旧版未命中只做两件事：换一句 placeholder、让主题区闪一下。用户还得自己在 14 张卡里找。

现在会给出**最接近的 3 个主题**，点一下直接进。

⚠️ **这里有个必须避开的坑**：不能拿 `scoreTopics()` 的结果当候选排序依据。
能走到"未命中"就说明**所有**主题得分都是 0（只要有一个 >0 就已经命中了），
用它筛候选必然得到空列表——我第一版就是这么写的，逻辑上不可能有候选。

所以另写了一个 `similarTopics()`，用**二元组（bigram）重合度**算相似度：
把用户输入切成相邻两字的二元组，与「主题名 + 该主题全部关键词」的二元组集合求重合比例，取前 3。

- 它**只用来猜**，用户点了才跳转，不存在"静默给错结果"的风险——这是它敢用模糊算法的前提
- 标签文案也照实说："没有完全对上。你说的更接近这几个？"
- 跑在 `showNoMatch()` 里，只在未命中路径执行，不影响主流程性能

### 6.4 补充问题的答案怎么用（2026-09-25 新增）

**改之前：答案收上来就被丢掉了。** `state.answers` 只在点击时写进去，
`showResult()` 从来没读过它——用户答完一题，结果页和没答时一模一样。提问纯属摩擦。

现在答案有三个出口，全部由数据字段驱动（`optionHints` / `escalateFrom`，见 §4）：

| 出口 | 做法 |
|---|---|
| **回显** | 结果页顶部显示"你现在更需要什么：缓解方法"，让用户看到自己答了什么 |
| **书区排序** | `optionHints[i] === "popular"` → 把「近年热门」区提到「经典书」前面 |
| **风险升级** | `escalateFrom` 命中 → 风险提示框里追加一段「先求助、不必等看完书」 |

`escalateFrom` 只用在 3 道**安全相关**的题上（`mood` 持续时间 / `personality` 如何求助 /
`health` 就医经历）——这直接对应 §12 那条"涉及严重心理议题时，求助引导永远排在书单前面"。

---

## 7. 怎么改、怎么发版

工作区目录**不是** git 仓库，线上内容在 GitHub。标准流程：

```powershell
# 1) 在 kunjing-shudan-app/ 里改文件（唯一编辑入口）

# 2) 拉一份仓库副本（临时目录，每次重新克隆最稳）
git clone https://github.com/nvnv19309/kunjing-shudan.git "$env:TEMP\kunjing-sync"

# 3) 把改好的文件覆盖进去
#    ⚠️ 仓库根目录除了 5 个应用文件，还有这份 AGENTS.md（它不在 kunjing-shudan-app/ 里），
#       所以必须分别复制，只 Copy-Item kunjing-shudan-app\* 会把 AGENTS.md 漏掉
Copy-Item "<工作区>\kunjing-shudan-app\*" "$env:TEMP\kunjing-sync\" -Force
Copy-Item "<工作区>\AGENTS.md"          "$env:TEMP\kunjing-sync\" -Force

# 4) 确认差异无误后提交（git 身份未全局配置，需用 -c 内联指定）
git -C "$env:TEMP\kunjing-sync" -c user.name="nvnv19309" -c user.email="nvnv19309@users.noreply.github.com" add -A
git -C "$env:TEMP\kunjing-sync" -c user.name="nvnv19309" -c user.email="nvnv19309@users.noreply.github.com" commit -m "描述这次改了什么"
git -C "$env:TEMP\kunjing-sync" push origin main

# 5) 验证
git -C "$env:TEMP\kunjing-sync" rev-list --left-right --count HEAD...origin/main   # 应输出 0  0
```

**发版前自查**：
- 推送前用 `Compare-Object` 或去空白后比对，确认工作区与仓库内容一致（历史上踩过"以为没推，其实只是行尾 CRLF 差异"的坑）。
  仓库已加 `.gitattributes`（`* text=auto eol=lf`），git 会自动把 CRLF 归一化为 LF 后入库，这类幽灵 diff 已从根源消除；
  如果 `git status` 又出现莫名其妙的整文件修改，先检查 `.gitattributes` 是否还在。
- 推送后等约 1 分钟，再开线上地址确认。
- `books-data.js` 是纯数据文件，语法错误会导致整个页面白屏——改完用 node 加载一次验证：
  ```powershell
  node -e "const fs=require('fs');let s=fs.readFileSync('books-data.js','utf8').replace('const BOOKS_DATA','globalThis.BOOKS_DATA');eval(s);console.log('themes='+Object.keys(BOOKS_DATA).length)"
  ```

---

## 8. 当前数据统计（截至 2026-09-24）

- **主题数**：14
- **书籍总数**：62（经典书 30 + 近年热门 32）
- **难度分布**：入门 32 / 进阶 24 / 较难 6
- **出版年份跨度**：1932 – 2025
- **预警主题**：5 个（`mood` / `personality` / `health` / `family` / `breakup`）
- **关键词总数**：376（2026-09-25 点算）
- **评分完整性**：豆瓣分 **62 / 62 齐全**；Goodreads 分 **57 / 62**——
  有 5 本在 Goodreads 上查无条目，`goodreads` 字段写占位符 `"—"`，页面显示为「Goodreads —」。
  这 5 本：`career`/《非线性成长》、`money`/《小狗钱钱》、`family`/《为何家会伤人》、
  `sensitivity`/《多向思考者：高敏感人群的内心世界》、`breakup`/《我们为什么不爱了》。

  > 早期文档曾写「62 本全部有豆瓣分与 Goodreads 分（无缺失）」，与数据不符，已按实际情况更正。
  > 用 `"—"` 表示无该平台条目**不违反**硬约束 4（评分必须真实）——问题只在统计口径。

主题清单（顺序即页面卡片顺序）：

| # | key | 主题 | 经典/热门 | 关键词数 | 预警 |
|---|---|---|---|---|---|
| 1 | `anxiety` | 焦虑与压力 | 2 / 2 | 28 | |
| 2 | `procrastination` | 拖延与执行力 | 1 / 2 | 19 | |
| 3 | `career` | 职业迷茫与选择 | 2 / 2 | 18 | |
| 4 | `relationship` | 人际关系与社交 | 2 / 2 | 16 | |
| 5 | `learning` | 学习方法与效率 | 2 / 2 | 21 | |
| 6 | `mood` | 情绪低落与抑郁倾向 | 3 / 4 | 43 | ✅ |
| 7 | `self` | 自我认知与成长 | 2 / 2 | 14 | |
| 8 | `personality` | 人格障碍与自我怀疑 | 4 / 4 | 44 | ✅ |
| 9 | `money` | 金钱与现实困境 | 2 / 2 | 29 | |
| 10 | `meaning` | 人生意义与虚无感 | 2 / 2 | 15 | |
| 11 | `health` | 健康焦虑与就医恐惧 | 2 / 2 | 34 | ✅ |
| 12 | `family` | 原生家庭与童年创伤 | 2 / 2 | 37 | ✅ |
| 13 | `sensitivity` | 高敏感与完美主义 | 2 / 2 | 32 | |
| 14 | `breakup` | 失恋与情感创伤 | 2 / 2 | 26 | ✅ |

> 关键词数已按数据文件点算到 2026-09-25（270 → 326 → **376**）。
> 改动关键词后**记得同步这张表**，否则接手的人会按错的数量做判断。
> 改完跑 `verify-kunjing.js`，它会拿这张表的数字跟数据文件逐项对，不一致会标 `<<< DIFF`。
>
> **`breakup` 的 `warning: true` 是 2026-09-25 加的**（此前一直空着，挂了两天等人拍板）。
> 它的标题里就带「创伤」，按硬约束 2 本来就该有；
> 被背叛 / 被抛弃 / 分手后的极端情绪也确实可能需要先引导求助。

> 有 5 本书跨主题复用（如《被讨厌的勇气》同时出现在 `relationship` / `self` / `sensitivity`），
> 这是**有意为之**——同一本书能对应不同困境入口，不视为重复。

---

## 9. 代码整洁度与体积基线

### 9.1 体积基线（2026-09-25 第四次重测，发布前）

| 文件 | 原始 | gzip | 占传输量 |
|---|---|---|---|
| `books-data.js` | 40,034 B | 12,757 B | 51% |
| `style.css` | 12,214 B | 3,522 B | 14% |
| `app.js` | 17,906 B | 6,463 B | 26% |
| `index.html` | 5,771 B | 2,445 B | 10% |
| **合计** | **75,925 B** | **25,187 B** | 100% |

GitHub Pages 自动走 gzip 传输，**真实传输体积以 gzip 列为准（约 25.2 KB）**。
要重新测量，用 `System.IO.Compression.GZipStream` 压一遍再取长度即可，别只看原始字节数。

**体积变化史**（gzip 口径）：

| 里程碑 | 原始 | gzip | 说明 |
|---|---|---|---|
| 清理死代码后（09-24 早） | 59,703 B | 19,536 B | 删 6 个未用 CSS 变量 |
| 补关键词 + 并列规则（09-24 晚） | 61,728 B | 20,422 B | 词表 270 → 326 |
| 关键词 + 输出逻辑（09-25 早） | 71,630 B | 23,687 B | 词表 326 → 376、候选/回显/排序/升级 |
| **本轮（09-25 发布前）** | **75,925 B** | **25,187 B** | +`breakup` 预警、本地使用日志（含导出/清除） |

> 本轮 gzip 再涨 1,500 B（+6.3%）。
> **累计三天从 19.5 KB 涨到 25.2 KB（+29%）**——换来的东西：匹配准确率翻倍、
> 6 个死词复活、答案真的影响输出、未命中有候选、求助引导能按情况升级、
> 数据只存本地可导出可清除。**这仍是一笔划算的交易，25 KB 对一个单页站不算负担。**

### 9.2 「压缩代码」已评估过，结论：不做 minify

实测过全量 minify（去注释 + 压空白后重新 gzip）的收益：

| 项 | 数值 |
|---|---|
| minify 后 gzip 合计 | 17,677 B |
| 相比现状节省 | **2,176 B（11%）** |
| 代价 | `books-data.js` 变成不可读 —— 它占传输量最大，且是后续加书、改推荐理由时唯一要动的文件 |

> 上表测于旧基线（59,703 B / gzip 19,536 B）。之后两轮改动把基线推到 71,630 B / gzip 23,687 B，
> 绝对数字会变，但**结论（约 11%、不值得做）不受影响**；而且本轮 `app.js` 的体积主要来自注释，
> minify 会正好删掉最该留的那部分。

**结论：不值得，不要再试。** 为了 11% 的传输收益牺牲内容可维护性，与"项目价值在内容质量、不在技术"的定位直接冲突。
根因是 gzip 已经把缩进和重复空白压得很干净，minify 的边际收益极小。

**如果将来真要减传输体积**，唯一有实质效果的方案是**按主题拆包按需加载**：
`books-data.js` 独占 61% 传输量，拆成 14 个主题文件后，首屏只需加载主题列表（约 1 KB），
点选主题时再异步加载该主题数据。代价是要改架构、维护 14+ 个数据文件。

### 9.3 死代码现状：已清空

| 类别 | 数量 | 状态 |
|---|---|---|
| CSS class | 53 | 全部被 `index.html` / `app.js` 引用 |
| CSS 变量 | 19 | 定义与 `var()` 引用 **1:1 完全对应**，无死变量 |
| JS 函数 | 12 | 全部被调用，无死函数 |
| HTML id | 13 | 全部被 JS 引用（`step-input` / `step-question` / `step-result` 由 `"step-" + name` 拼接引用） |
| `@keyframes` | 2（`pulse` / `fadeIn`） | 均在使用 |

> 本轮清理移除了 6 个从未被 `var()` 引用的变量：
> `--bg-deep`、`--border-soft`、`--brand-soft-strong`、`--accent-soft`、`--warning`、`--shadow-lg`。

**维护约定**：
- 新增 CSS 变量就要用，定义了不用等于死代码。
- **删变量前必须确认全站没有 `var(--xxx)` 引用**——否则样式会静默失效（浏览器不报错，只是颜色/阴影丢失，很难排查）。
- 扫描死代码时注意两个坑：`@import` 的字体 URL 会被误判成 CSS class（`googleapis` / `com`），
  而 `step-input` 这类由字符串拼接的 id 会被误判成未引用。都是假阳性。

---

## 10. 已知缺口 / 下一步可做

1. **主题覆盖仍偏"情绪与关系"**：成瘾、进食障碍、职场霸凌、亲子关系（已为人父母视角）等高频困境尚未覆盖。
2. **关键词匹配的泛化能力有天花板（09-24 首测 / 09-25 重测）**

   测试集结构（每套 14 主题 × 5 条口语输入）：

   | 集合 | 用途 | 纪律 |
   |---|---|---|
   | `kw-cases.js` + `kw-holdout.js` = **调参集**（141 条） | 用来选词 | 可以随便看、随便改 |
   | `kw-v3.js` = **验收集**（70 条） | 衡量真实泛化 | **看过它的失败明细就不能再拿它验收** |
   | `kw-negative.js` = **负例集**（A 类 12 条完全无关 + B 类 8 条易误触发） | 测误命中 | 期望"未命中" |

   > `kw-holdout.js` 原本是 09-24 的留出集，因为当时已看过它的失败明细，
   > 09-25 起**降级为调参集**，另写 `kw-v3.js` 作为新的验收集。这是流程要求，不是洁癖。

   | 阶段 | 调参集 | v3 验收集 | 负例A 误命中 |
   |---|---|---|---|
   | 09-24 收尾（326 词） | 62.4% | 30.0% | 0/12 |
   | 修大小写死词 + 去冗余 + 删泛词 + 补词（376 词） | **97.2%** | **42.9%** | 0/12 |

   **真实收益是 v3 上的 +12.9 个百分点。** 调参集那个 97.2% 不能当成绩看——
   它是拿这套题选出来的词拟合出来的。想再往上走只有三条路：
   - 继续补词 —— 边际收益递减，且**必须留一套没调过的数据来验证**
   - 加一层轻量模糊匹配 —— 注意 `similarTopics()`（§6.3）已经在未命中路径用了二元组相似度，
     但那**只用来猜候选，不参与判定**；要是让它参与判定，就得重新做一轮验收
   - **先做埋点**（见下条），拿真实用户输入决定补哪里 —— 数据驱动，性价比最高

   ⚠️ **两次踩到的同一个坑：数据泄漏。**
   - 09-24：看过留出集的失败明细之后才写补词表，分数从 25.7% 虚高到 55.7%
   - 09-25：写完 `kw-v3.js` 才列补词表，v3 分数从 30% 虚高到 78.6%

   两次都是"把答案抄进了训练集"。**硬规则：补词表里每个词都必须原文出现在调参集的失败句里**，
   写完后逐个回查一遍。复跑方式见 §13.1。

3. **负例 B 类有 3/8 无法修**（结构性问题，不是 bug）：
   「帮我把钱转给房东」→ `money`（靠「钱」）、「明天陪我妈去医院体检」/「公司安排了年度体检」→ `health`（靠「体检」）。
   裸子串匹配做不到"这个词在这里是不是在说困境"，除非删掉「钱」「体检」这些高频词——
   但删了会把「没钱交房租」「不敢去体检」这些真阳性一起弄丢。**取舍是保留真阳性，接受这类假阳性。**

4. **无收藏 / 无分享**：目前没有"把书单存下来"的能力，用户看完即走。
5. **无埋点**：不知道哪些主题被点得多，无法用数据决定下一步补哪个主题。
   本轮把候选机制和答案回显都做了，**埋点是最有价值的下一步**——它能直接告诉你该补哪个主题的词。
6. **主题卡片为平铺网格**：14 个主题在小屏上需要滚动，可考虑分组（情绪 / 关系 / 成长 / 现实）。

---

## 11. 修改速查

| 想做的事 | 改哪里 |
|---|---|
| 新增一个困境主题 | `books-data.js` 加一个 key（`title` / `icon` / `keywords` / `questions` / `classic` / `popular`）；涉及严重议题记得 `warning: true`。**卡片会自动出现，无需改 HTML** |
| 给某主题加书 | 在对应 `classic` 或 `popular` 数组里加 book 对象 |
| 某个说法匹配不到 | 往对应主题的 `keywords` 里加口语化说法（**别照抄测试集的句子**，见 §10.2） |
| 改补充问题 / 选项 | `books-data.js` 对应主题的 `questions`；**改 `options` 时必须同步改 `optionHints`**（等长、一一对应），否则 `verify-kunjing.js` 会报错 |
| 改「哪个选项让热门书排前面」 | `optionHints` 里的 `"popular"` |
| 改「哪个选项触发求助升级」 | `escalateFrom`（下标从 0 数） |
| 改求助升级的文案 | `index.html` 的 `#warning-extra` |
| 未命中的候选要几个 | `app.js` 里 `similarTopics(text, 3)` 的第二个参数 |
| 调整配色 / 字体 | `style.css` 顶部 `:root` 的 CSS 变量（已有注释说明每个颜色的意图） |
| 改风险提示文案 / 热线号码 | `index.html` 的 `#warning-box` |
| 改免责声明 | `index.html` 的 `.site-footer` |
| 改推荐理由的措辞 | `books-data.js` 各 book 的 `reason` 字段 |

---

## 12. 交接给下一位的注意事项

- **改数据前先备份**：`books-data.js` 是单文件数据库，一个逗号错误整站白屏。
- **不要重构成框架**：这个项目的价值在**内容质量**（书选得对不对、理由写得贴不贴心），不在技术。
  引入 React / 构建工具只会增加维护成本，收益为零。
- **内容调性 = 产品本身**：温暖、不说教、第二人称、承认困境的真实性。
  新增内容的文字质量，直接决定这个产品好不好用。
- **优先引导求助，而非卖书**：涉及严重心理议题时，求助引导永远排在推荐书单前面。

---

## 13. 回归方式与已知技术债

### 13.1 核对脚本（`verify/` 目录，不参与部署）

上一级目录的 `verify/` 里放着一整套**只读**核对脚本。
**不要把它们复制进 `kunjing-shudan-app/`** —— 那个目录是线上应用的同步源，多一个文件就会被推到仓库。

| 脚本 | 作用 |
|---|---|
| `verify-kunjing.js` | 加载 `books-data.js`，核对主题/书籍/难度/年份/预警/评分完整性/关键词数/`optionHints` 长度，并**与本文档 §8 的数字逐项对照** |
| `deadcode-check.js` | 死代码统计（CSS class / 变量 / JS 函数 / HTML id / keyframes）+ §3.1 行数对照 |
| `kw-matrix.js` | 匹配质量实验台。三个变体（现状 / 修大小写 / 本轮词表）在**调参集 + v3 验收集 + 两套负例**上的对照表。**唯一能证明匹配改动是否真的有效的东西** |
| `kw-cases.js` + `kw-holdout.js` | **调参集**（各 70 条，共 141 条）。可以随便看、随便改 |
| `kw-v3.js` | **v3 验收集**（70 条）。上一轮之后写的，**调参期间不要看它的失败明细** |
| `kw-negative.js` | 负例集：A 类 12 条完全无关（期望未命中）+ B 类 8 条含易误触发词 |
| `kw-audit.js` | 逐词审计：死词（含大写 ASCII）/ 冗余词 / 跨主题重复词 / 单字词 |
| `short-kw-scan.js` | 专门扫单字词与「被同主题更长词包含的冗余词」 |
| `kw-debug.js` | 打印一句话在某个变体下**每个主题的命中明细**（排错用，`node kw-debug.js "你的句子"`） |
| `zi-check.js` | 「自己」这类跨主题泛词的专项验证 |
| `apply-kw-changes.js` | 把 DEL / ADD 词表写进 `books-data.js`（保持单行数组格式） |
| `apply-hints.js` | 给 14 组问题的选项写入 `optionHints` / `escalateFrom` |
| `e2e-kunjing.js` | 核心流程端到端：14 主题全流程 + 关键词匹配 + 未命中路径 + 无 JS 报错 |
| `e2e-interactions.js` | **输出逻辑专项端到端**：监听器不累积 / 未命中候选 / 点候选跳转 / 书区排序 / 选择回显 / 风险升级 / 返回后状态重置 / 无 404 |
| `listener-leak.js` | 实测 `#question-list` 上监听器的累积情况（见 13.2，现已修复） |

```bash
export PATH="/usr/bin:/bin:$PATH"
export NODE_PATH="C:/Users/91924/.workbuddy/binaries/node/workspace/node_modules"
NODE="C:/Users/91924/.workbuddy/binaries/node/versions/22.22.2-3/node.exe"
cd <项目目录>/verify

"$NODE" verify-kunjing.js            # 数据与文档对照 → 应无 DIFF
"$NODE" deadcode-check.js            # 死代码与行数 → 应全 OK
"$NODE" kw-matrix.js                 # 匹配质量（两个集合 + 两套负例）
"$NODE" e2e-kunjing.js --local       # 核心流程本地回归 → 111 PASS / 5 FAIL
"$NODE" e2e-interactions.js --local  # 输出逻辑本地回归 → 21 PASS / 0 FAIL
"$NODE" e2e-kunjing.js               # 发版后打线上，确认部署生效
"$NODE" e2e-interactions.js          # 同上
```

**改关键词或匹配逻辑后，必须做这四件事：**
1. 跑 `kw-matrix.js`，看 **v3 验收集**那一列有没有退步 —— 只看调参集等于自欺
2. 跑两套 `e2e-*.js --local`，确认失败项没有增加
   （当前基线：核心 **111 PASS / 5 FAIL**，5 条全部是 §8 那 5 本无 Goodreads 条目的书；
   输出逻辑 **21 PASS / 0 FAIL**）
3. 同步本文档 §8 的关键词数表与 §3.1 的行数
4. 新词逐个回查「它是不是从验收集的句子里抄来的」——这是唯一挡住数据泄漏的动作

### 13.2 已修的技术债（留档，避免重复踩）

| 问题 | 修于 | 怎么修的 |
|---|---|---|
| `renderQuestions()` 对常驻容器 `#question-list` 反复绑 click，换一次主题 +1 个监听器（实测 1→2→3→4→5） | 09-25 | 所有事件监听集中到 `init()` 里绑一次 |
| 匹配时 `text` 转小写但关键词没转 → `BPD` / `NPD` / `ASPD` / `CPTSD` / `PUA` / `HSP` **6 个死词**永远匹配不上 | 09-25 | `scoreTopics()` 里把 `kw` 也 `toLowerCase()`，并按小写形态去重 |
| 未命中只改一句 placeholder，用户还得自己在 14 张卡里找 | 09-25 | 加 `similarTopics()` 二元组相似度，给最接近的 3 个候选供点击（§6.3） |
| 补充问题的答案收上来就被丢掉，`showResult()` 从不读 `state.answers` | 09-25 | `optionHints` 排序书区 + `escalateFrom` 升级风险提示 + 结果页回显（§6.4） |
| 站点无 favicon，线上 `/favicon.ico` 返回 404 | 09-25 | `index.html` 加 `<link rel="icon" href="data:,">` |

**仍然存在的（轻微）**：`.gitattributes` 缺末尾换行符（末字节 `0x66`，`wc -l` 报 2 而实际 3 行）。
无实际影响，有余力时补一个换行。

### 13.2.1 写脚本时踩过的两个坑（与产品代码无关，但很容易再犯）

1. **对象字面量里重复的 key 会被静默覆盖。** 我在 `kw-matrix.js` 的补词表里把 `career`
   写了两次（一次来自错配项、一次来自未命中项），JS 直接丢掉前一个 —— 一半补词根本没生效，
   而脚本照常输出数字，没有任何报错。**结果就是"改了却没用，还以为自己改对了"。**
   凡是按"来源分类"往同一个表里堆词，写完一定检查 key 是否唯一。
2. **别拿"关键词得分"去给未命中路径排候选。** 能走到未命中的前提就是所有主题得分都是 0，
   所以那个列表必然是空的。这类"逻辑上不可能有结果"的分支，跑起来不会报错，只会永远不出东西。

### 13.3 遗留的判断项（需产品决策，不要自行改）

- **`breakup`（失恋与情感创伤）没加 `warning: true`**。硬约束 2 写的是
  「涉及抑郁、自伤、人格障碍、**创伤**、成瘾、进食障碍的主题都必须加」，
  它的标题里就带「创伤」。但它不是临床意义的创伤，加不加属于产品判断，留给人决定。
- **`optionHints` 的映射是我的判断，不是实测结论。** 哪一类选项算"想要方法"（→ 热门书优先）
  没有数据支撑，是照选项语义定的。这份映射全在数据里，改哪一条都是一行的事。
- **文本匹配的整体水平仍偏低**（v3 验收集 42.9%）。主题卡片点击仍是主要路径，
  匹配只是便利功能。要不要投入做语义匹配、还是先加埋点看真实输入，见 §10.2。
- **`kw-debug.js` 与 `kw-matrix.js` 的 DEL/ADD 表是两份拷贝**，改词表时要同步，
  否则调试结论跟实验结论会对不上。（没合并是因为 `kw-matrix.js` 要用多种变体对比，
  而 `kw-debug.js` 只跑单一变体、追求最短路径。）